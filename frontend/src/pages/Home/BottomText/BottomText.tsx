import React, {FC, ReactElement} from "react";

import "./BottomText.css";

const BottomText: FC = (): ReactElement => {
    return (
        <div className="bottom-text">
            <h1>
                Elevate.
            </h1>
        </div>
    );
};

export default BottomText;

